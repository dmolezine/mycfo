{
  "name": "mycfo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test",
    "check": "npm run test && npm run build",
    "supabase": "supabase"
  },
  "dependencies": {
    "@supabase/supabase-js": "2.112.4"
  },
  "devDependencies": {
    "supabase": "2.115.0",
    "vite": "8.2.2"
  },
  "engines": {
    "node": ">=22"
  }
}
